source 'https://rubygems.org'

ruby '3.3.0'

gem 'rails', '~> 7.1.2' # Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"

gem 'bootsnap', require: false # Reduces boot times through caching; required in config/boot.rb
gem 'cssbundling-rails' # Bundle and process CSS
gem 'jbuilder' # Build JSON APIs with ease
gem 'jsbundling-rails' # Bundle and transpile JavaScript
gem 'pg', '~> 1.1' # Use postgresql as the database for Active Record
gem 'puma', '>= 5.0' # Use the Puma web server
gem 'redis', '>= 4.0.1' # Use Redis adapter to run Action Cable in production
gem 'sprockets-rails' # The original asset pipeline for Rails
gem 'stimulus-rails' # Hotwire's modest JavaScript framework
gem 'turbo-rails' # Hotwire's SPA-like page accelerator
gem 'tzinfo-data', platforms: %i[windows jruby] # Windows does not include zoneinfo files, so bundle the tzinfo-data gem

# gem "image_processing", "~> 1.2" # Use Active Storage variants
# gem "kredis" # Use Kredis to get higher-level data types in Redis
# gem "bcrypt", "~> 3.1.7" # Use Active Model has_secure_password

group :development, :test do
  gem 'debug', platforms: %i[mri windows] # This library provides debugging functionality to Ruby
  gem 'pry-rails' # Gem which causes rails console to open pry.
  gem 'rubocop-rails' # A RuboCop extension focused on enforcing Rails best practices and coding conventions.
end

group :development do
  gem 'web-console' # Use console on exceptions pages [https://github.com/rails/web-console]

  # gem "rack-mini-profiler" # Add speed badges [https://github.com/MiniProfiler/rack-mini-profiler]
  # gem "spring" # Speed up commands on slow machines / big apps [https://github.com/rails/spring]
end
